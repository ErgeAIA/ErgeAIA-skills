# 触发测试集

> 用于 description 修改后的回归验证。正面集应触发调用，负面集不应触发。

## 正面触发集（应触发调用）

| 编号 | 用户输入 | 预期判定 | 判定依据 |
|------|----------|----------|----------|
| P1 | "帮我写个更新日志" | ✅ 触发 | 含关键词"更新日志" |
| P2 | "初始化 CHANGELOG" | ✅ 触发 | 含关键词"CHANGELOG" + 初始化意图 |
| P3 | "生成 changelog" | ✅ 触发 | 含关键词"changelog" |
| P4 | "发布版本，写 release notes" | ✅ 触发 | 含关键词"release notes" + 发布意图 |
| P5 | "记录这次变更" | ✅ 触发 | 含关键词"变更" + 记录意图 |
| P6 | "从 git log 生成发版记录" | ✅ 触发 | 含关键词"发版记录" + git log 意图 |
| P7 | "版本历史怎么写" | ✅ 触发 | 含关键词"版本历史" + 求助意图 |
| P8 | "+init" | ✅ 触发 | 直接使用快捷命令 |
| P9 | "+add 新增了登录功能" | ✅ 触发 | 直接使用快捷命令 |
| P10 | "+release 1.0.0" | ✅ 触发 | 直接使用快捷命令 |
| P11 | "+generate" | ✅ 触发 | 直接使用快捷命令 |
| P12 | "+check" | ✅ 触发 | 直接使用快捷命令 |

## 负面触发集（不应触发调用）

| 编号 | 用户输入 | 预期判定 | 判定依据 |
|------|----------|----------|----------|
| N1 | "git commit 怎么写" | ❌ 不触发 | 属于 git commit message 规范，非 changelog |
| N2 | "创建一个 git tag" | ❌ 不触发 | 属于 git tag 创建，非 changelog 维护 |
| N3 | "发布 GitHub Release" | ❌ 不触发 | 属于 GitHub Release 创建，非 changelog 内容维护 |
| N4 | "写一篇技术博客" | ❌ 不触发 | 属于博客写作，非 changelog |
| N5 | "帮我写 README" | ❌ 不触发 | 属于 README 文档，非 changelog |
| N6 | "代码审查一下" | ❌ 不触发 | 属于代码审查，非 changelog |
| N7 | "帮我翻译这个文档" | ❌ 不触发 | 属于翻译任务，非 changelog |
| N8 | "多语言版本的更新日志怎么管理" | ❌ 不触发 | 属于多语言版本管理，明确在 Non-Goals 中排除 |

## 边界案例（需澄清）

| 编号 | 用户输入 | 预期判定 | 处理方式 |
|------|----------|----------|----------|
| B1 | "帮我写个日志" | ⚠️ 需澄清 | "日志"可能指 changelog 或运行日志，需询问 |
| B2 | "记录一下项目变化" | ⚠️ 需澄清 | "变化"可能指 changelog 或其他记录方式，需询问 |
| B3 | "生成版本说明" | ⚠️ 需澄清 | 可能指 changelog 或独立 release notes，需询问 |

## 测试执行方式

1. 将正面集逐条输入 Agent，验证是否触发 changelog-manager
2. 将负面集逐条输入 Agent，验证是否不触发 changelog-manager
3. 边界案例应触发澄清询问而非直接调用或拒绝

## 回归验证触发条件

当修改 SKILL.md 的 description 字段后，应重新执行本测试集验证触发行为是否符合预期。